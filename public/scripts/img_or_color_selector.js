$(document).ready(function () {    $("#selector").change(function () {        var val = $('#selector').val();        var send = {            img_or_color: val        };        $.ajax({            data: send,            method: 'POST',            url: 'detect/',            success: function () {                if(val == 0){                    $('.img_or_color').text('');                    $('.img_or_color').append('Changed to image').fadeIn(500).delay(750).fadeOut(500);                }else{                    $('.img_or_color').text('');                    $('.img_or_color').append('Changed to color').fadeIn(500).delay(750).fadeOut(500);                }            }        })    });});